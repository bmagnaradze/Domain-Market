export interface IDomainFilter {
  symbolRange: number[];
  selectedCategories: string[];
  selectedDomains: string[];
  search: string;
  minPrice: number;
  maxPrice: number;
  minSymbolLenght: number;
  maxSymbolLenght: number;
}
